#include <bits/stdc++.h>
using namespace std;

int main(){
    int tc; cin >> tc;
    while(tc--){
        int n, b;
        float a; cin >> n >> a >> b;
        cout << 1.0 * n * a + b << endl;
    }

    return 0;
}